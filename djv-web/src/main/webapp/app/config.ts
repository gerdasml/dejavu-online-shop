export const config = {
    productsPerPage: 30, // TODO: if extension is needed, use multiples of 30
    searchQueryParamName: "q",
    adminTableScrollWidth: {
        common: 900,
        properties: 500
    }
};
